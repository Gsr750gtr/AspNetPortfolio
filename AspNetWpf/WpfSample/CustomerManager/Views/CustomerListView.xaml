<UserControl x:Class="CustomerManager.Views.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Command="{Binding RefreshCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh"/>
                        <TextBlock Text="更新"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding InsertCommand}" Margin="10,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Add"/>
                        <TextBlock Text="追加"/>
                    </StackPanel>
                </Button>
            </ToolBar>
            <ToolBar Band="1">
                <DockPanel>
                    <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20" VerticalAlignment="Center"/>
                    <TextBox Width="200" VerticalAlignment="Center" Margin="5,0" materialDesign:HintAssist.Hint="検索"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=500}" />
                </DockPanel>
            </ToolBar>
        </ToolBarTray>

        <DataGrid ItemsSource="{Binding CustomerInfos}" AutoGenerateColumns="False" SelectionMode="Single" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="取引先コード" Binding="{Binding Code}" />
                <DataGridTextColumn Header="取引先名" Binding="{Binding Name}" />
                <DataGridTextColumn Header="名称カナ" Binding="{Binding NameKana}" />
                <DataGridTextColumn Header="都道府県" Binding="{Binding Prefecture}" />
                <DataGridTemplateColumn Header="削除">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}" Background="PaleVioletRed">
                                <materialDesign:PackIcon Kind="Delete"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>

</UserControl>
